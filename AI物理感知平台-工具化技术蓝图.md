# AI 物理感知平台：工具化技术蓝图

> **口径（必须先读）**：「物理」在本文件中 **不是** 世界模型 / 物理定律 / Occupancy 语义，而是 **视频、音频、文字之外的物理传感器信号**。  
> 平台对象：LiDAR、毫米波/4D 雷达、超声波、IMU/GNSS、力/力矩、触觉、振动、温度、压力、电流、应变、Wi‑Fi CSI、RFID、事件相机、红外等。  
> 视频 / 音频 / 文本可以作对齐锚或人机接口，**不是本平台的感知主模态**。  
> 时间锚点：2026 年中。产业窗口：**G3 单模态传感器基础模型 → G4 异构物理信号统一 token 化**。

---

## 0. 定义、边界与平台定位

**AI 物理感知** = 把非视听文的传感器信号，变成可同步、可标定、可表征、可融合、可评测的智能感知能力。

它解决的是大模型时代的结构性缺口：VLM / LLM 已经吃掉图像、视频、声音和文字，**点云、雷达立方、IMU 序列、力触觉、振动频谱仍停留在「每个传感器一个小模型」**。

| 不在范围内 | 在范围内 |
| --- | --- |
| RGB 视频理解、文生视频、ASR、NLP | 测距：LiDAR / 雷达 / 超声波 / ToF / UWB |
| 纯视觉 Occupancy / 世界视频生成 | 惯性与本体：IMU、编码器、电机电流 |
| 以像素观感（FID/FVD）为北极星 | 接触：力/力矩、触觉阵列、压强、应变 |
| 再做一个多模态聊天机器人 | 工况：振动、温度、压力、流量、气体 |
| | 射频与隐私传感：mmWave 人体、Wi‑Fi CSI、RFID |
| | 非 RGB 光电：事件相机、红外/热成像 |

**平台化原则：**

1. **信号原生**：先处理波形、点、立方、税素（taxel），不要先把一切画成假图像再喂给 ViT（除非该传感器本身就是成像式触觉）。  
2. **时间与标定是一等公民**：异构传感器的时钟、外参、采样率比模型结构更先决定能不能融。  
3. **缺失模态必须可工作**：雷达被雨衰、触觉没贴上、IMU 丢包，系统不能崩。  
4. **工具即产品**：接入、同步、分模态 tokenizer、对齐、融合、评测各自可交付。  
5. **场景垂直、底座水平**：智驾测距、工业工况、接触作业共用同一信号操作系统。

---

## 1. 代际演进思路

### 1.1 演进总表：从哪一代到哪一代

主线不是「视觉模型更大」，而是 **物理信号从滤波特征 → 单模态深度网络 → 任务型融合 → 传感器基础模型 → 统一物理 token**。

| 代际 | 时间窗口 | 核心问题 | 典型技术 | 输出 | 产业状态 |
| --- | --- | --- | --- | --- | --- |
| **G0 滤波与手工特征** | ~2000–2015 | 信号怎么稳住？ | Kalman/EKF、FFT/包络、阈值、栅格地图 | 轨迹、频谱峰、开关量 | 工控/车身已商品化 |
| **G1 单模态深度学习** | ~2015–2020 | 这一路传感器能不能端到端？ | PointNet/VoxelNet、雷达谱图 CNN、IMU LSTM、振动 1D-CNN | 检测框、HAR 标签、故障类别 | 单任务可用 |
| **G2 任务型多传感融合** | ~2018–2023 | 几路信号怎么接到一个任务头？ | 前/中/后融合、BEV 激光+雷达、多通道 PdM | 检测/定位/告警 | 智驾与工业主力 |
| **G3 传感器基础模型** | ~2021–2025 | 无标签物理信号能否学通用表征？ | LIMU-BERT、UniTouch/Sparsh、雷达占用网络、点云 MAE | 可迁移 embedding | 研究转早期产品 |
| **G4 异构物理 token 化** | **2024–2027（当前）** | 任意物理传感器能否进同一表示总线？ | Babel 可扩展对齐、HoloLLM 稀有模态注入、Digit 360 多传感指尖、OctoSense 多模态 MAE | 统一 PhysToken + 缺模态推理 | **平台窗口** |
| **G5 物理传感操作系统** | 2027+ | 新传感器能否即插即用、自标定、自评测？ | 编码器注册表、时钟织物、缺失模态路由 | 感知 OS + 场景包 | 早期构想 |

**本平台选择：以 G2 为量产交付形态，以 G3 为算法底座，主攻 G4（统一物理 token + 缺失模态），预埋 G5 的传感器注册与自标定接口。**

不重做 G0 的 Kalman 库；不把平台做成「再训练一个 YOLO」；不把 RGB 视频当主输入。

### 1.2 一条演进主线（必须钉死）

```
原始物理信号（点 / 波形 / 立方 / 税素 / CSI）
  → 手工特征与滤波（稳、准、可解释）
    → 单模态深度表征（这一路传感器自己会看）
      → 任务型融合（几路信号服务同一个 KPI）
        → 自监督传感器基础模型（无标签也能迁任务）
          → 统一物理 token 总线（新传感器可注册，缺一路仍能推理）
```

三次不可省略的跃迁：

1. **从「工程师抽特征」到「网络学表征」**（G0→G1）  
   振动不再只看 RMS/峭度，IMU 不再只靠阈值计步，LiDAR 不再只靠聚类。代价是：标定、采样率、量纲必须进数据契约，否则深度模型不可复现。

2. **从「一路传感器一个模型」到「异构融合」**（G1→G2）  
   车端 LiDAR×4D 雷达×IMU，产线振动×电流×温度，机械臂力觉×触觉×关节。融合失败的主因通常不是网络，而是 **时钟不同步、外参漂移、采样率差两个数量级**。

3. **从「为每个任务重训融合头」到「物理信号基础模型」**（G3→G4）  
   LIMU-BERT 证明 IMU 可自监督预训练；Sparsh/UniTouch 证明触觉可跨传感器迁移；Babel 把 Wi‑Fi / mmWave / IMU / LiDAR 做可扩展对齐；HoloLLM 把稀有传感器注入语言接口。G4 的判据是：**新加一路物理传感器，不必从零堆一个任务网络**。

### 1.3 模态地图（平台要覆盖的信号家族）

| 家族 | 典型信号 | 物理量 | 主要失效模式 |
| --- | --- | --- | --- |
| 测距与空间 | LiDAR 点云、4D 雷达立方、超声波、ToF | 距离、径向速度、反射率 | 雨雾尘、低反射、多径、稀疏 |
| 惯性与本体 | IMU 六轴、磁力计、GNSS、关节角、电机电流 | 加速度、角速度、位姿、力矩代理 | 漂移、磁干扰、遮挡失锁、量化噪声 |
| 接触 | F/T、光学/电容/磁触觉、压强 | 力、滑移、接触几何、纹理 | 磨损、温漂、标定失效、凝胶老化 |
| 工况 | 振动加速度、温度、压力、电流、声发射 | 频谱、热、负荷 | 变工况混淆故障、采样不够高 |
| 射频感知 | mmWave 人体、Wi‑Fi CSI、RFID | 微动、存在、身份/距离 | 环境多径、隐私与法规、标注稀缺 |
| 非 RGB 光电 | 事件相机、红外 | 亮度变化、热辐射 | 与 RGB 工具链不兼容、标定难 |

视频、音频、文字：允许作为 **对齐教师**（例如 Babel 用部分成对数据）或 **解释层**（HoloLLM 式语言接口），禁止成为训练与评测的主输入。

### 1.4 代际失败模式

| 若停留在 | 失败表现 |
| --- | --- |
| G0 | 换设备、换工况即失效；无法消费无标签天级数据 |
| G1 单模态 | 雨天雷达可用但模型没接；接触任务全靠视觉猜 |
| G2 无同步 | 融合在 PPT 里存在，车上/产线上对不齐，比单传感器更差 |
| G3 无统一接口 | 每个传感器一个仓库、一套归一化，平台化失败 |
| G4 仍绑 RGB | 物理信号被画成伪图像硬塞 ViT，带宽浪费、量纲丢失 |
| 只刷视觉榜 | 看起来多模态，实际消融掉雷达/力觉分数几乎不变 |

---

## 2. 关键指标抽象

物理传感器不能沿用 mAP（图）或 WER（声）。需要 **按信号家族可插拔、按层可追溯** 的 Metrics OS。

### 2.1 六层指标金字塔

```
L6 商业结果     停机小时、接管率、一次装配成功、备件周转、单位传感器小时成本
L5 任务闭环     检测/插入/HAR/RUL 任务成功；缺模态时的跌幅
L4 融合与对齐   跨模态检索、时间对齐误差、可靠性加权是否踩对失效传感器
L3 单模态智能   点云检测、雷达速度、IMU 轨迹、滑移 F1、故障 F1
L2 信号契约     SNR、丢包、采样抖动、量纲/量程、标定残差
L1 系统工程     端到端时延、功耗、时钟同步误差、热漂移
```

**原则：每一层只回答一个问题。L5 好看但 L2 时钟差 20 ms，融合结果作废。**

### 2.2 指标字典（分家族，平台内建）

#### L1–L2 所有传感器共用（准入门禁）

| 指标 | 含义 | 熔断经验 |
| --- | --- | --- |
| 时钟同步误差 | 多传感器同一事件的时间差 | 超任务时延预算（智驾常 <10–20 ms）则禁止融合 |
| 外参/手眼/安装标定残差 | 几何是否还成立 | 超阈禁止进训练与发布 |
| 丢包率 / 采样抖动 | 信号是否完整 | 超过协议 SLA 降级为单模态 |
| 量纲与量程合规 | g、rad/s、N、m、dB 是否写进 schema | 不合规数据不得入库 |

#### L3 单模态（按家族激活）

**测距**

| 指标 | 用途 |
| --- | --- |
| 点云 3D mAP / NDS（nuScenes 类） | LiDAR 检测 |
| 距离 RMSE、径向速度 RMSE | 雷达动力学 |
| 4D 占用 IoU、虚警率 Pd/Pfa | 雷达/激光空间占用 |
| 恶劣天气 AP 跌幅 | 物理传感相对摄像头的存在理由 |

**惯性 / 本体**

| 指标 | 用途 |
| --- | --- |
| ATE / RPE | 轨迹（ milliradian / 米） |
| HAR / 姿态 F1、少样本精度 | IMU 基础模型是否可迁 |
| 航向漂移 °/h | 无 GNSS 时段 |

**接触**

| 指标 | 用途 |
| --- | --- |
| 滑移检测 F1、提前量（ms） | 抓取稳定 |
| 接触力 RMSE、接触位姿误差 | 插装/打磨 |
| 跨传感器迁移落差 | DIGIT vs GelSight vs 磁皮肤是否共用表征 |

**工况**

| 指标 | 用途 |
| --- | --- |
| 故障分类 F1、误报率 | 振动/电流诊断 |
| RUL MAE / Score | 剩余寿命 |
| 故障提前量（天） | 是否赶得上排程 |

**射频**

| 指标 | 用途 |
| --- | --- |
| 存在/计数/动作准确率 | mmWave / CSI |
| 跨环境跌幅 | 多径泛化 |

#### L4 融合与对齐（G4 核心）

| 指标 | 定义 |
| --- | --- |
| 缺模态跌幅 Δ | 拿掉雷达/力觉/IMU 后 L5 下降；下降太小说明没真用物理信号 |
| 跨模态检索 R@k | IMU↔LiDAR、触觉↔力等 embedding 是否对齐（Babel 范式） |
| 可靠性门控准确率 | 雨天是否压低摄像头、抬高雷达（RAF 类） |
| 多速率对齐误差 | 1 kHz 力觉 vs 10 Hz 点云是否对上同一接触事件 |

#### L5–L6 任务与商业（场景包激活）

只在对应 Scene Pack 打开，不写进基础 tokenizer 的训练 loss。

### 2.3 平台宪法（指标工具化）

1. **先契约后模型**：没有 schema（采样率、单位、坐标系、时间基）的数据包，Curator 拒收。  
2. **缺模态是一等测试**：每个发布必须报「全模态 / 去雷达 / 去力觉 / 去 IMU」四列。  
3. **禁止视觉分数冒充物理感知**：消融物理通道后 L5 变化 < 约定阈值，不得宣称「多传感融合」。  
4. **指标即 API**：`evaluate(run_id, modalities[]) → MetricsBundle`，CI 与客户报告共用。

---

## 3. 对标业界产品思路

对标对象是 **传感器栈、融合中间件、传感基础模型**，不是 Cosmos 式世界视频平台。

### 3.1 对标地图

| 产品 / 体系 | 代际 | 产品形态 | 可借鉴 | 不可照搬 |
| --- | --- | --- | --- | --- |
| **华为乾崑 ADS** | G2 量产 | LiDAR + 4D 毫米波 + 超声波 + IMU，多传感器融合 | 测距家族工程闭环、恶劣天气互补、车规时延 | 不做整车智驾；摄像头不是本平台主模态 |
| **NVIDIA Drive / Isaac** | G2–G3 | 传感器抽象、时间同步、雷达/激光驱动、机器人力控接口 | 传感器 SDK、时钟、仿真回放 | 不绑 NVIDIA 全家桶为唯一运行时 |
| **Meta Digit + Sparsh + Digit Plexus** | G3–G4 | 触觉硬件 + 跨传感器表征 + 手部软硬件总线 | **物理传感工具化标杆**：硬件、表征、连接器一起做 | 不自研整只灵巧手 |
| **GelSight 工业检测** | G2 | 接触几何/纹理计量，航空与精密制造 | 触觉信号直接变成质检 KPI | 不做成单一光学触觉盒子 |
| **Bosch / 大陆 / 法雷奥 / Arbe** | G1–G2 | IMU、毫米波、4D 成像雷达、超声波 | 车规传感器物理极限与接口 | 不造传感器芯片 |
| **华为云 / 西门子 / GE PdM** | G1–G2 | 振动+温度+电流的预测性维护 | 工况家族数据网关、边缘推理、RUL 闭环 | 不做大而全工业云 |
| **高通 Sensor Hub / 手机 IMU 栈** | G1–G2 | 低功耗常开惯性传感 | 边缘侧物理信号 OS、少算力 | 不进消费电子整机 |
| **Babel / LIMU-BERT 开源栈** | G3–G4 | IMU 预训练 + 可扩展多传感对齐 | **平台算法内核**：分模态塔 + 可追加模态 | 研究原型，缺车规/工规 |
| **HoloLLM** | G4 | LiDAR/红外/mmWave/Wi‑Fi 注入 MLLM | 稀有传感器进统一接口的工程套路 | 语言层是插件，不是产品本身 |
| **ImageBind 等 VLM 多模态** | — | 把 IMU 当第六模态塞进视觉对齐 | 反面教材：传感模态又少又弱 | 不以图像为对齐中心 |

### 3.2 真正对手盘：三层产品结构

业界能规模化的不是「又一个点云网络」，而是下面三层。本平台按这三层拆工具。

| 层 | 产业先例 | 本平台模块 |
| --- | --- | --- |
| **接入与时间** | DriveWorks、ROS 2、工控网关、Digit Plexus | **PhysIngest**：协议、PTP/时钟、标定、schema |
| **分模态表征** | LIMU-BERT、Point Transformer、Sparsh、雷达占用网络 | **PhysTokenizer**：点 / 立方 / 窗口序列 / 税素 / 频谱 |
| **对齐与融合** | ADS 融合、Babel 可扩展对齐、RAF 可靠性门控 | **PhysAlign + PhysFusion + PhysEval** |

架构信号：

- **先同步，再融合，再表征升级**（G2 工程未完成时，不要空谈 G4）。  
- **每类传感器一个 tokenizer，不要一个 ViT 打天下**。成像式触觉可以走图像编码器，IMU/振动必须走时序/频谱编码器。  
- **新模态用可扩展对齐接入**（Babel：不必 N 路全配对），而不是每次推倒重训。

### 3.3 三条产品思路，只选一条主航道

| 思路 | 代表 | 收入逻辑 | 态度 |
| --- | --- | --- | --- |
| A. 物理信号操作系统 | Digit Plexus、Drive 传感器栈、Sensor Hub | 卖接入、同步、tokenizer、评测 | **主航道** |
| B. 垂直整机感知 | 华为 ADS、主机厂智驾 | 卖行车体验 | 可做中间件，不做整车 |
| C. 单一传感器硬件 | 禾赛、Arbe、ATI、GelSight | 卖器件 | 集成，不自研芯片/探头 |

**结论：做 A。用 TOP3 场景证明「缺模态仍可用 + L5 真用了物理通道」。硬件与整机留给生态。**

### 3.4 竞争策略一句话

> 别人把雷达和力觉画成图，塞进视觉大模型；我们把物理信号当成一等模态：有时钟、有量纲、有 tokenizer、有缺模态评测。

---

## 4. TOP3 商业场景（2026 SOTA 重排）

筛选：物理通道不可替代、**已有出货或法规**、付费方清晰、能反哺分传感器 tokenizer。咨询公司「百亿美元市场」不采信。

### 4.1 场景一：座舱儿童遗留 / 乘员存在（60 GHz 雷达 × 车身 IMU）

**为什么第一：** 2026 年最硬的非视听文商用窗口是法规，不是体验。Euro NCAP 2026 把 Child Presence Detection 列为五星必需，且必须 **直接传感、行程默认开启**（Safe Driving / SD-102：盖毯、脚窝、新生儿到 6 岁）。C-NCAP 2027 已列「儿童遗忘提醒」。毯子、安全座椅壳体、关灯停车场是摄像头的结构性盲区，隐私也不允许舱内常开录像。

**已出货：** TI AWRL6844（2025-01-06）单芯片做安全带提醒 + CPD + 入侵，TI 称乘员定位约 98%、儿童分类超过 90%，相对重量垫+超声大约少 20 美元/车。Vayyar 60 GHz 与 VinFast VF 6/7 合作。加特兰称 2026 Q1 车规毫米波累计出货超 3000 万颗。

**平台切入：** 雷达微多普勒 tokenizer + 车辆抖动 IMU 门控 + 去雷达必须掉分。不输出开空调/解锁/方向盘。

**L6：** 盖毯漏检、空座虚警、驻车功耗。  
**客户：** 主机厂安全电子、座舱 Tier1。

### 4.2 场景二：UWB 数字钥匙安全测距（UWB ToF × 手机 IMU）

**为什么第二：** 这是双向飞行时间 + 密码学，不是「看见人」。BLE 单模会被中继攻击。CCC Digital Key 已是开放标准；NXP 公开材料写明 Apple 自 iPhone 11 带 UWB，宝马 / Genesis / 蔚来 / 比亚迪已上数字钥匙。ST 厂商口径：UWB 器件 2025 年 5.27 亿颗 → 2030 年 14 亿。同一颗 UWB 正在叠加雷达模式做 CPD / 踢脚感应（ST64UWB-A500、加特兰 Dubhe），tokenizer 能复用，任务头不能混。

**平台切入：** UWB CIR / 距离-角度 tokenizer + 「走近 vs 袋里晃」的 IMU。证伪：拿掉 UWB，只剩 BLE 必须能被中继骗过。

**L6：** 开锁距离误差、中继攻击成功率、口袋误开。  
**客户：** 主机厂车身电子 + 手机 OEM。

### 4.3 场景三：家居 / 楼宇毫米波人感（60 GHz 雷达）

**为什么第三、而不是「暗光门锁 TOF」：** 2026 年真正铺货的是存在传感器。Aqara FP2（60–64 GHz，约 40 m²、最多 5 人、浴室 IPX5）已零售；Google Soli 从 Pixel 4 走到 Nest Hub / 温控器；楼宇照明与 HVAC 按在室人数调负荷。PIR 只能看「动没动」，静坐灭灯是雷达替换 PIR 的商业理由。卫生间、卧室开不了摄像头。

与场景一物理同类（毫米波微动），客户和 Eval 完全不同：车规卷宗 vs Matter / 退货。同一座雷达塔，两套 Scene Pack。

**L6：** 静坐漏检、门外虚警、隐私投诉。  
**客户：** 家居品牌、楼宇照明 / HVAC 集成商。

### 4.4 暂不进 TOP3

| 场景 | 原因 |
| --- | --- |
| 手机贴身交互（接近光×TP×IMU） | 出货最大，但 Sensor Hub / Core Motion 已做成封闭 G2，没有独立中间件付费方 |
| 车外 4D 成像雷达 | 开支最大（Continental 累计 2 亿颗雷达、Freetech 近百万定点），责任绑行车，只作观察 |
| 工业振动 / 电流 PdM | ROI 清，协议碎，能复用时序塔再升 |
| 机器人力触觉 | Digit 360 / Sparsh 热，量产连接器与付费未收口 |
| 可穿戴健康 IMU | 医疗合规与渠道主导 |
| 纯视觉 / 文生视频 | 不是物理传感器问题 |

三条 TOP 共享：**雷达 / UWB / IMU 的 Ingest + 分传感器 tokenizer + 缺模态评测**；差异在法规卷宗、安全测距和消费虚警。

---

## 5. TOP3 研究团队

标准：是否把 **非视听文传感器** 当作一等模态、是否有可复用表征/硬件接口、是否对平台架构有直接输入。

### 5.1 第一：Meta FAIR Robotics（DIGIT / Sparsh / Digit Plexus）

- **代表人物：** Jitendra Malik、Mustafa Mukadam、Francois Robert Hogan 等；产业侧联合 GelSight、Wonik。  
- **代表工作：** DIGIT → Digit 360（指尖级多传感：接触成像、压力、IMU 等）；Sparsh 跨传感器触觉表征；Sparsh‑X 融合触觉图/压力/IMU；Digit Plexus 把手部多种触觉接到同一软硬件总线。  
- **对平台的输入：** **物理传感要工具化，必须硬件连接器 + 表征 + 任务头一起设计**；跨厂家触觉迁移是 G4 必考题。  
- **差距：** 重心在手与触觉，不覆盖车载雷达/工业振动。平台应抄其「Plexus」形态，扩展到测距与工况家族。

### 5.2 第二：港科大 Mo Li 团队 × 传感基础模型谱系（LIMU-BERT → UniHAR → Babel）

- **代表工作：** LIMU-BERT（SenSys 2021，IMU 自监督基础模型，移动端可部署）；UniHAR；Babel（与微软亚洲研究院等：Wi‑Fi / mmWave / IMU / LiDAR 等可扩展模态对齐，不必 N 路全配对）。LIMU-BERT 已有即时配送等业务落地经验论文。  
- **对平台的输入：** **分模态塔 + 可追加对齐** 就是 PhysTokenizer / PhysAlign 的算法原型；IMU 证明物理时序信号可以不靠视频做基础模型。  
- **差距：** 研究原型距车规/工规工程（时钟、功能安全、产线协议）仍远。平台负责把对齐算法嵌进 Ingest 与 Eval。

### 5.3 第三：南洋理工大学 MARS Lab（HoloLLM，杨剑飞等）

- **代表工作：** HoloLLM（NeurIPS 2025）：把 LiDAR、红外、mmWave、Wi‑Fi 等 **稀有物理模态** 注入多模态大模型；用 Universal Modality-Injection Projector 处理表征异构与成对文本稀缺。  
- **对平台的输入：** G4 的工程现实是 **对齐数据极缺、信号形态极不一样**；需要「注入器」而不是幻想一个万能 ViT。语言接口可作可选解释层。  
- **差距：** 偏人体感知与语言评测，工业力觉/振动未覆盖。作稀有模态接入的方法库，不作产品形态。

### 5.4 观察名单

| 团队 | 处理方式 |
| --- | --- |
| MIT Adelson / GelSight 学术源头 | 光学触觉物理与计量，算法补给 |
| CMU + UW（UniTouch、Sparsh 合作） | 跨触觉传感器预训练 |
| 清华 / 上交 / 西工大等车载雷达激光实验室 | 测距家族算法与恶劣天气融合 |
| Bosch Corporate Research、Arbe 等 | 4D 雷达物理与量产约束 |
| 国内人形创新中心、智元等 | 接触数据协同，不绑定单一本体 |

---

## 6. 平台工具化架构

### 6.1 分层工具栈

```
┌─────────────────────────────────────────────────────────────┐
│  Scene Packs                                                │
│  座舱儿童遗留  │  UWB 数字钥匙        │  房间毫米波人感      │
├─────────────────────────────────────────────────────────────┤
│  PhysFusion + 可靠性门控（缺模态路由）                       │
├──────────────┬──────────────┬──────────────┬────────────────┤
│ PhysAlign    │ PhysPredict  │ 可选语言头   │ 可选策略头     │
│ 可扩展对齐   │ 短时信号外推 │ 解释，非主路径│ 场景侧，非底座 │
├──────────────┴──────────────┴──────────────┴────────────────┤
│  PhysTokenizer  ★ 分家族编码器                              │
│  雷达立方 │ UWB CIR │ IMU 窗 │ TOF │ 接近光/TP（预训练子集） │
├─────────────────────────────────────────────────────────────┤
│  PhysEval：契约 + 单模态 + 缺模态 + 任务                     │
├─────────────────────────────────────────────────────────────┤
│  PhysIngest ★ 时间 / 标定 / schema / 协议                   │
│  PTP·PPS · 外参 · 单位量程 · 丢包 · ROS2/车载/工控总线       │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 六个可独立售卖的工具

| 工具 | 代际职责 | 最小交付 | 指标层 |
| --- | --- | --- | --- |
| **PhysIngest** | G2 工程底座 | 时钟同步、标定、schema 拒收、录包回放 | L1–L2 |
| **PhysTokenizer** | G3 | 雷达 / UWB / IMU / TOF 分传感器编码器 + 归一化 | L3 |
| **PhysAlign** | G4 | 可追加模态对齐、雷达↔UWB 迁移 | L4 |
| **PhysFusion** | G2→G4 | 可靠性加权融合、缺模态推理 | L4–L5 |
| **PhysEval** | 宪法 | 缺模态五列、盖毯 CPD / 中继攻击 / 静坐人感插件 | L1–L5 |
| **Scene Packs** | 商业 | 座舱 CPD / 数字钥匙 / 房间人感 三包 | L6 |

**明确不做：** RGB 视频大模型、ASR、通用对话、整车规控、自研激光器/雷达芯片/凝胶指尖。

### 6.3 核心接口

```text
SensorFrame = {
  modality,          # lidar | radar4d | imu | ft | tactile | vib | csi | ...
  t_utc, t_hw,       # 统一时间基 + 硬件时间
  pose_extrinsic,    # 安装外参
  unit, range, rate, # 量纲、量程、采样率
  payload,           # 点 / 立方 / 序列 / 税素 / 频谱
  quality            # SNR、丢包、过热、饱和
}

PhysToken_t = Tokenizer[modality](SensorFrame)
State_t     = Fusion(Align({tokens}), mask_missing)
Evaluate(run, drop_modalities=[...]) -> MetricsBundle
```

新传感器准入：实现 Ingest 驱动 + Tokenizer + Eval 插件，不必改 Fusion 主干。这是 G5 预埋。

### 6.4 技术路线图（18 个月）

| 阶段 | 时间 | 目标 | 必须交付 |
| --- | --- | --- | --- |
| P0 | 0–3 月 | G2 立柱 | PhysIngest（同步+schema）；选定一个 Scene Pack 打通录包→评测 |
| P1 | 3–9 月 | G3 | 该场景的 Tokenizer + PhysEval 缺模态列；L5 证明物理通道非摆设 |
| P2 | 9–18 月 | G4 | PhysAlign 接入第二家族（如测距后再接触，或工况）；第二 Scene Pack |
| P3 | 并行 | G5 槽位 | 传感器注册表、自标定流水线接口，不承诺万能即插即用 |

P0 场景建议：**座舱儿童遗留**（法规刚需、责任边界是「有没有生命体」不是方向盘）；UWB 数字钥匙作第二包。

---

## 7. 决策摘要

1. **物理 = 非视听文传感器信号。** 平台不是世界模型公司，也不是视觉公司。  
2. **代际：** G2 融合工程为交付，G3 表征为底座，主攻 G4 统一物理 token 与缺模态。主线是信号契约 → 单模态智能 → 异构融合 → 可注册的基础模型。  
3. **指标：** 时钟/标定/量纲是熔断项；发布必须报缺模态跌幅；禁止用视觉榜冒充物理感知。  
4. **对标：** 抄 Digit Plexus 的工具化、抄 ADS 的测距融合工程、抄 LIMU-BERT/Babel 的分模态塔与可扩展对齐。  
5. **场景：** 座舱儿童遗留、UWB 数字钥匙、房间毫米波人感。手机贴身交互降为预训练子集。  
6. **团队：** 座舱雷达（TI / Vayyar / 加特兰）对 Ingest；Mo Li / Babel 对分传感器塔；Soli / HoloLLM 对房间人感与注入。  
7. **产品：** PhysIngest + PhysTokenizer + PhysEval，而不是把雷达画成视频去刷 VLM。

---

## 附录 A. 术语

| 术语 | 含义 |
| --- | --- |
| 物理传感器信号 | 除视频、音频、文字外的测量信号（点云、雷达、IMU、力、振动等） |
| Taxel | 触觉像素 / 阵列单元 |
| 4D 雷达 | 距离×方位×俯仰×速度的成像雷达 |
| CSI | Wi‑Fi 信道状态信息，可作隐私友好的人体传感 |
| ATE / RPE | 绝对/相对轨迹误差，惯导与里程计常用 |
| RUL | 剩余使用寿命 |
| 缺模态 | 某路传感器失效或未安装时系统仍需工作 |
| Tokenizer | 把该家族原始信号编成可融合的 token，而非特指文本分词 |

## 附录 B. 公开锚点

- LIMU-BERT（SenSys 2021）：IMU 自监督基础模型  
- Babel：Wi‑Fi / mmWave / IMU / LiDAR 可扩展模态对齐  
- Meta DIGIT / Digit 360 / Sparsh / Digit Plexus：触觉硬件与跨传感器表征  
- HoloLLM（NeurIPS 2025）：稀有物理模态注入 MLLM  
- 华为乾崑：量产 LiDAR + 4D 毫米波融合  
- OctoSense、OmniVLA、VTLA/MLA：机器人侧非 RGB 传感进入策略  
- 工业 PdM：振动/温度/电流 + 边缘推理与 RUL  

---

*相对旧版蓝图的修正：删除以 Occupancy / 世界视频 / Cosmos 为中心的「物理 AI」叙事。旧 PPT 若仍按世界模型口径，以本文件为准并已同步重生成。*
